export const baseUrl = 'http://localhost:3000/';

//configuation setting that we can use on other parts of our app 
//this is the base url of the JSON server 
//we can use this const whereever in our code where we need to give this base server address - 
//so if server address ever changes we only need to change it once in this file